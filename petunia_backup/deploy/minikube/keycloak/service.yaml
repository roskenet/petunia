apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    app: keycloak
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: service
      protocol: TCP
      name: http
  selector:
    app: keycloak

